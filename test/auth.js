const test = require('ava');

test('User successful login', t => {
	// const res = await 
})
test('User gets 403 on invalid login')
test('User get 401 if token depricated')
test('User can take new access token using refresh token')
test('Refresh tokens become invalid on logout')
test('User can have multiple refresh tokens')